#include <bits/stdc++.h>
using namespace.std


// declare function to determine factorial value of some integer n

int fact(n) {

  if(n == 1) {
  
    // returns factn (the int value we are assigning the factorial of n to) when n = 1 (the end of the factorial multiplication
  
    return n;
    
  } else {
  
    // multiplies n*(n-1)*(n-2)*...*(1)
    
    factn = n * fact(n-1)
    
  }

}

int main() {

  int n;
  cin >> n;
  fact(n);

  return 0;
  
}
